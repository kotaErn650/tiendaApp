<?xml version="1.0" encoding="UTF-8" ?>
<Application
    x:Class="ShopApp.App"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:ShopApp.Converters"
    xmlns:models="clr-namespace:ShopApp.Models">

    <Application.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Converter global -->
            <converters:PrecioParaColorConverter x:Key="PrecioParaColorConverter" />

            <!-- DataTemplate global para Product -->
            <DataTemplate x:Key="ProductDataTemplate" x:DataType="models:Product">
                <Grid Padding="10" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                    <Label
                        Grid.Row="0"
                        Grid.Column="0"
                        FontAttributes="Bold"
                        FontSize="16"
                        Text="{Binding Nombre}" />
                    <Label
                        Grid.Row="0"
                        Grid.Column="1"
                        FontAttributes="Bold"
                        FontSize="16"
                        HorizontalTextAlignment="End"
                        Text="{Binding Precio, StringFormat='{0:C}'}"
                        TextColor="{Binding Precio, Converter={StaticResource PrecioParaColorConverter}}" />
                    <Label
                        Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        FontSize="13"
                        Text="{Binding Descripcion}"
                        TextColor="Gray" />
                </Grid>
            </DataTemplate>

        </ResourceDictionary>
    </Application.Resources>

</Application>
